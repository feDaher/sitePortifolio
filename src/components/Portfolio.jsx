import styled from "styled-components";

const StyledContainer = styled.div`
  height: 100vh;
`
const Portfolio = () => {
  return (
    <StyledContainer id="portfolio">Portfolio</StyledContainer>
  )
}

export default Portfolio